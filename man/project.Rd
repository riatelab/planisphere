% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project}
\alias{project}
\title{Project geographic data using a D3.js projection}
\usage{
project(
  x,
  proj = "geoAzimuthalEqualArea",
  rotate = NULL,
  reflectX = NULL,
  reflectY = NULL,
  scale = 500,
  center = NULL,
  clip = TRUE,
  graticule = c(10, 10),
  additional_layers = FALSE,
  verbose = FALSE,
  ct = .planisphere$ct,
  ...
)
}
\arguments{
\item{x}{An \code{sf} spatial dataframe to project.}

\item{proj}{A D3 projection name or constructor (e.g. "geoInterruptedHomolosine").}

\item{rotate}{Rotation parameters passed to the projection.}

\item{reflectX}{Logical; whether to reflect the projection on the X axis.}

\item{reflectY}{Logical; whether to reflect the projection on the Y axis.}

\item{scale}{Projection scale factor (D3 spherical scale, not planar CRS units).}

\item{center}{Optional projection center.}

\item{clip}{If TRUE, clips the projected geometries to the projected sphere.}

\item{graticule}{Numeric vector of longitude/latitude step size for graticule generation.}

\item{ct}{A custom V8 JavaScript context if needed. See \code{new_v8_context()}.}

\item{...}{Additional parameters passed to the projection builder.}

\item{additional_ayers}{Logical. If TRUE, adds graticule and sphere layers. In this case, the function returns a list. If FALSE (default), it returns a spatial data frame.}
}
\value{
If \code{additionalLayers = FALSE}, a single \code{sf} object corresponding to the projected basemap.

If \code{additionalLayers = TRUE}, a list of \code{sf} objects:
\itemize{
  \item \code{basemap}: projected input geometries
  \item \code{sphere}: outline of the projected globe
  \item \code{graticule}: projected graticule lines
}
}
\description{
Projects spatial data using D3.js geographic projections executed in a V8 JavaScript context.
The projection is performed in spherical coordinates, following D3.js design principles,
where the Earth is modeled as a sphere rather than an ellipsoid.
}
\details{
D3 geographic projections are based on spherical geometry. All transformations assume a
perfect sphere, which differs from traditional GIS pipelines that often rely on ellipsoidal
datums (e.g., WGS84). As a result, this function is primarily intended for visualization
and cartographic rendering rather than high-precision geodetic computation.
}
\examples{
library(sf)
world <- st_read(
  system.file("gpkg/land.gpkg", package = "planisphere"),
  quiet = TRUE
)

result <- planisphere::project(x = world, proj = "geoInterruptedBoggs")

}
\seealso{
\code{\link{init}}
}
